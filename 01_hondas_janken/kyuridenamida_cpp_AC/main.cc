#include <bits/stdc++.h>

using namespace std;

// 00:36 -> 00:38

int main() {
    string s;
    cin >> s;
    if (s.front() != s.back()) {
        cout << "honda" << endl;
    }else{
        cout << "draw" << endl;
    }
}
