#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

signed main(int argc, char* argv[]) {
  registerValidation(argc, argv);
  inf.readWord("[A-Z]{2}");
  inf.readEoln();
  inf.readEof();
  return 0;
}
